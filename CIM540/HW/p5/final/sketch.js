
var images = [];
var wordprocessor = "assets/wp.jpg";
var wp = {};


function preload(){

  }
  wp = loadImage(wordprocessor);
  console.log(wp);

    createCanvas(1280, 480);
    var start = createButton("Click to be an awesome writer");
      }
  }

function setup() {
  createCanvas(1280, 480);

}

function draw() {
  clear();
  background(wp);

}
